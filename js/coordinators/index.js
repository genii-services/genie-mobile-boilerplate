const { useAuth } = require("./auth")
const { useRouter } = require("./router")
const { useSettings } = require("./settings")
const { useStyle } = require("./style")

module.exports = {
	useAuth,
	useRouter,
	useSettings,
	useStyle,
}
